<!DOCTYPE html>
<html lang="en">
<head>
  <title>Login</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-r from-blue-900 to-indigo-800 font-sans">

  <div class="relative w-full max-w-sm p-6 bg-white/10 backdrop-blur-md rounded-2xl shadow-xl">
    <!-- Decorative Circles -->
    <div class="absolute -top-6 -left-6 w-24 h-24 bg-blue-500/30 rounded-full"></div>
    <div class="absolute -bottom-6 -right-6 w-32 h-32 bg-indigo-500/30 rounded-full"></div>

    <h1 class="relative text-2xl font-extrabold text-center text-white mb-6">Welcome Back</h1>

    <form action="/login" method="POST" class="relative flex flex-col space-y-4">
      <input
        type="email"
        name="email"
        placeholder="Email"
        required
        class="w-full px-4 py-3 bg-white/20 placeholder-white/70 rounded-lg focus:bg-white/30 focus:outline-none focus:ring-2 focus:ring-blue-300 transition"
      />

      <input
        type="password"
        name="password"
        placeholder="Password"
        required
        class="w-full px-4 py-3 bg-white/20 placeholder-white/70 rounded-lg focus:bg-white/30 focus:outline-none focus:ring-2 focus:ring-blue-300 transition"
      />

      <button
        type="submit"
        class="w-full py-3 bg-blue-600 hover:bg-blue-700 active:bg-blue-800 transition text-white font-semibold rounded-lg"
      >
        Log In
      </button>
    </form>

    <p class="relative mt-6 text-center text-sm text-white/80">
      Don’t have an account?
      <a href="/register" class="text-blue-300 hover:underline">Sign up</a>
    </p>
  </div>

</body>
</html>
